import React from "react";
import "../styles/HeaderFederalTaxInfo.css";

export const HeaderFederalTaxInfo = ({ children }) => {
  return <section className="headerTextWrapper">{children}</section>;
};
